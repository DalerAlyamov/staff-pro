const auth = Object.freeze(
  Object.seal({
    template: "/template",
  })
);

const app = Object.freeze(
  Object.seal({
    home: "/",
  })
);

export const paths = Object.freeze(
  Object.seal({
    auth,
    app,
  })
);
