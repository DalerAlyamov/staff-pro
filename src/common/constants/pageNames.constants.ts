const auth = Object.freeze(
  Object.seal({
    template: "template",
  })
);

const app = Object.freeze(
  Object.seal({
    home: "Главная",
  })
);

export const pageNames = Object.freeze(
  Object.seal({
    auth,
    app,
  })
);
